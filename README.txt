How to Run yara-integrator
1. Make sure you have docker compose installed.
2. Run "docker compose up" in terminal. (this might need root permission)
3. Once the installation is done, go to http://localhost:3003 to start.